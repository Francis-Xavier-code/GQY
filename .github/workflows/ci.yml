name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: cargo check (app)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps (alsa for rodio/cpal)
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev pkg-config
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.97.1"
          components: rustfmt, clippy
      - name: Cache cargo & target
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      # 仅校验应用本体（lib+bin）。注意：src 内联 #[cfg(test)] 单测当前不通过编译（test rot），
      # 故这里不传 --all-targets，避免把测试代码问题混进「应用能否编译」的门禁。
      - name: cargo check
        run: cargo check

  test:
    name: cargo test (inline unit tests)
    needs: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps (alsa for rodio/cpal; rg for glob/grep tools)
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev pkg-config ripgrep
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.97.1"
      - name: Cache cargo & target
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - name: cargo test
        run: cargo test

  clippy:
    name: cargo clippy (report)
    needs: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install system deps (alsa for rodio/cpal)
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev pkg-config
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: "1.97.1"
          components: clippy
      - name: Cache cargo & target
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
      - name: cargo clippy (report only)
        run: cargo clippy -- -W warnings
